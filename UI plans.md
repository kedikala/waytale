# Waytale UI Plans

This is the living tracker for rebuilding the Waytale UI cleanup from the stable baseline commit `fccee15` (`Let users control map camera`). The previous cleanup work is preserved in `stash@{0}: waytale-ui-cleanup-before-reset`, but we will rebuild from this commit in small, scoped changes instead of applying the stash wholesale.

Each task should stay small enough to review and test independently. Update only the row for the task being changed unless a dependency or acceptance check is intentionally revised.

| Task | Status | Scope | Primary Files | Dependencies | Acceptance Checks |
| --- | --- | --- | --- | --- | --- |
| UI-0 Living Plan | Done | Restore this tracker from the stash and reset it for a rebuild from `fccee15`. | `UI plans.md` | None | File exists, reflects the current baseline, and tracks scoped UI cleanup work. |
| UI-1 Brand/Wake Copy | Done | Rename user-facing product surfaces to Waytale and change wake phrase display copy to `Hey Waytale`. Add shared app-brand constants where practical. Wake detection should accept common recognition variants such as `hey waytale`, `hey way tail`, and `hey way tell`. Keep internal target, module, and bundle identifiers unchanged unless required by build settings. | `app/api/*`, `ios/IcelandTourGuide/*`, docs | UI-0 | No user-facing `Iceland Guide`, `Iceland GPS Voice Tour Guide`, or `hey tour guide` remains except intentional internal names. Saying `Hey Waytale`, `hey way tail`, or `hey way tell` starts realtime before and during navigation. Stop/end phrases include Waytale variants such as `thanks Waytale`, `bye Waytale`, and `done Waytale`. |
| UI-2 Icon Assets | Done | Add iOS app icon assets/project settings. This task should not touch app logic. | `ios/IcelandTourGuide/Assets.xcassets`, `ios/IcelandTourGuide.xcodeproj/project.pbxproj` | UI-0 | iOS installed app icon shows the new brand. |
| UI-3 Main UI Declutter | Done | Hide demo/test/status-heavy controls from the default native screen while keeping them available in a diagnostics surface. | `DriveModeView.swift` | UI-1 | Default screen is navigation/map-first; diagnostics still exposes demo GPS, auto POI, cache/testing, and debug state. |
| UI-4 Realtime Visibility | Done | Add a compact visible `Waytale Live`/wake-listening indicator that does not cover directions or navigation controls. | `DriveModeView.swift` | UI-1, UI-3 | Indicator clearly shows realtime active/listening state; Ask/End controls and navigation controls remain tappable. |
| UI-5 Place Suggestions | Done | Improve Google Places autocomplete request shaping and rendering: Iceland restriction, location bias, session tokens, clearer subtitles, and distance/context when available. | `GoogleNavigationService.swift`, `GuideModels.swift`, `DriveModeView.swift` | UI-0 | Queries such as `blu`, `hotel`, `bonus`, `vik`, and `airport` return place-first Iceland suggestions with useful subtitles; route selection still works by `placeId` or query. |
| UI-6 Web/PWA Retirement | Done | Remove the Next/PWA UI entirely. The Next app is backend-only route handlers for the iOS app; non-API paths return JSON `404`. | `app/page.tsx`, `app/route.ts`, `proxy.ts`, `components/*`, `hooks/*`, `public/*`, `package.json` | UI-1 | No PWA manifest, service worker, web app shell, React UI components, web icon, or frontend map/voice panel code remains. `/api/realtime/session` still returns a realtime client secret. |
| UI-7 Always-On Lifecycle | Done | Make the guide always-on after permissions while keeping internal `startDriveMode`/`stopDriveMode` lifecycle controls for diagnostics. | `TourGuideViewModel.swift`, `DriveModeView.swift`, `LocationManager.swift`, `VoiceQuestionService.swift` | UI-1, UI-3, UI-4 | App auto-activates GPS/geofences/audio/wake after permissions; wake phrase starts realtime before and during navigation. |
| UI-8 Realtime Greeting | Done | When a manual or wake-triggered realtime session opens with no initial question, make Waytale say `Hello, how can I help?` without changing `/api/realtime/session` shape. | `RealtimeGuideService.swift`, `app/api/realtime/session/route.ts` | UI-1 | Starting realtime without a question gives one short greeting; follow-up speech and end-session commands still work. |
| UI-9 Diagnostics Surface | Done | Preserve existing debug/demo/test controls behind a diagnostics surface so cleanup does not remove useful development flows. | `DriveModeView.swift` | UI-3 | Demo GPS, auto POI toggle, cache/testing controls, status tiles, and debug details remain reachable outside the default first screen. |
| UI-10 Navigation Realtime Reliability | Done | Revisit the navigation-specific Ask/Waytale interaction without broad audio-session churn: keep navigation buttons tappable, keep Google voice muting scoped to active realtime only, and make realtime activation recover cleanly after Google spoken directions. | `DriveModeView.swift`, `TourGuideViewModel.swift`, `RealtimeGuideService.swift` | UI-1, UI-4 | In navigation, tapping Ask Waytale works on the first try after Google directions speak; saying `Hey Waytale` works after guidance audio; route guidance and ending realtime still work. |

## Current Baseline Notes

- Current backend shape: Next is API-only for iOS, with no PWA/web UI.
- Non-API web paths should return backend-only JSON `404`.
- The iOS Swift app is the only user-facing app surface.
